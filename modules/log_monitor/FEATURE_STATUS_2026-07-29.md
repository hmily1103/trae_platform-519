# log_monitor 功能完整度排期表（2026-07-29 · 代码核实版）

> 依据：对 alert_engine.py / selfheal.py / views.py / knowledge_base.py / 两个前端模板逐条核对代码后确认。
> 核心约束：**不做代码自动修改，只增强诊断与闭环**（人工在环）。

## 一、已完整（9 项，代码核实通过，无需排期）

| 功能项 | 关键位置 | 备注 |
|--------|---------|------|
| 日志监控与告警触发 | alert_engine.py / views.py | 含去噪、刷屏抑制、同事件聚类（#24） |
| AI 日志分析（L2/L3/L4） | agent.py | 根因/定位/影响/排查路径/建议 patch |
| Self-Heal 诊断链路 | views.py 异步回调 + SSE | 结果回写告警并实时推送 |
| 只读证据采集 | selfheal.py READONLY_PROBES | 5 类模板已补强（#26），黑名单护栏 |
| 动态上下文截取 | views.py _get_dynamic_context_around_alert | 分类型扩窗 + context_meta（#23） |
| 历史案例 / RAG | knowledge_base.py | 沉淀/检索/质量加权/命中计数（#27） |
| 人工确认闭环（L5） | acknowledge 接口 + 确认弹窗 | 三种结论 + 结构化补充字段 |
| 结果展示 | 两个模板四端同步 | 证据链三色徽章、置信度三级（#25） |
| Bug 报告导出 | views.py _build_bug_report | 含 L3/L4/证据链/历史案例章节 |

## 二、半完整（4 项，核实细节与排期建议）

| 功能项 | 当前状态（代码核实） | 问题点 | 优先级 | 近期补齐？ |
|--------|---------------------|--------|--------|-----------|
| 告警规则 regex | 前端下拉有 `regex` 选项（index.html:1792/1926），后端能存；但 `AlertRule.matches()`（alert_engine.py:62-121）只有 keyword/exception/anr/crash/level 分支，**type='regex' 落到末尾 `return False`** | **静默失败**：用户建了正则规则，界面显示正常，实际永远不触发，且无任何报错提示——比"不生效"更危险，会让用户误以为设备没问题 | **P0** | **是**。改动极小：加一个 regex 分支（约 5 行），或直接复用 keyword 分支（keyword 本身已支持 re.search）。建议顺带加"规则测试"预检 |
| 告警动作 action | 前端可配 screenshot/shell/custom_shell，后端 CRUD 完整（views.py:1353-1497 仅存取回显）；`action_taken` 字段（alert_engine.py:137）**全工程无任何赋值点**，无执行逻辑 | 纯预留设计。用户配置了动作会期待截图/dumpsys 发生，实际什么都不做 | **P1** | **建议做，但限只读白名单**：screenshot 与 dumpsys 类只读命令与"只增强诊断"约束不冲突（就是自动取证）；custom_shell 自由命令**不建议放开**，或必须过 _safe_adb 同款黑名单 |
| 自动关单 | 逻辑存在且受控：默认关闭（LOG_SELF_HEAL_AUTO_CLOSE=false，selfheal.py:314），高危永不自动关，仅高置信 AUTO_RESOLVED 才可关 | 覆盖低——但这是**有意设计**（人工在环约束），不是缺陷 | P2 | **否**。维持保守口径，属"受控能力"而非待补齐项。可后续加"自动关单准确率统计"再谈放宽 |
| 置信度体系 | 三信号规则模型（命中堆栈/已解决历史案例/上下文充足度，#25），高档口径与自动关单绑定 | 是规则化判断而非学习型评分；但样本量小时规则模型更可解释、更适合给领导讲 | P2 | **否**。等知识库积累 50+ 真实案例、有采纳率数据后再考虑升级为统计加权 |

## 三、未落地（6 项，与核心约束的关系）

| 功能项 | 状态 | 是否要做 |
|--------|------|---------|
| 自动读取业务代码定位修复点 | 无源码关联能力 | **可做（只读不改）**：给 Agent 配置只读源码路径，L3 定位到 `Foo.java:88` 时自动附上真实代码片段——增强诊断，不违反约束。建议列为下一阶段候选 |
| 自动修改代码 | 仅生成 suggested_patch 展示 | **不做**（核心约束，明确排除） |
| 自动执行修复 | 无执行链路 | **不做**（核心约束，明确排除） |
| 修复后自动验证 | 无 | **半可做**：不自动修，但可做"人工修复后一键复测"增强（已有复测按钮雏形，可加'复测通过自动更新案例 fixed_version'） |
| 多步任务规划 Agent（Planner） | 单次分析型 | 下一阶段（Agent 2.0）再议 |
| 多 Agent 协作 | 无框架 | 下一阶段（Agent 2.0）再议 |

## 四、建议排期结论

**近期只需要补 2 个**：
1. **P0：regex 规则分支**（半天内，含测试）——修复静默失败
2. **P1：action 只读动作执行**（1 天，含护栏与测试）——screenshot + 白名单 shell，产出直接挂到告警证据链

其余项要么是有意保守（自动关单/置信度），要么被核心约束排除（自动修代码），要么属于 Agent 2.0 范畴（Planner/多 Agent/源码只读关联）。
