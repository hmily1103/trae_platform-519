# Trae Platform - 核心依赖
Flask>=2.3.0
requests>=2.28.0

# API 文档 (OpenAPI/Swagger)
pyyaml>=6.0

# 自动化测试
pytest>=7.0.0

# 统一认证 + API 限流
Flask-Limiter>=3.5.0

# ARM 服务器压测 (SSH + 密码加密)
paramiko>=3.0.0
cryptography>=41.0.0

# 播放器压测报告 (图表)
matplotlib>=3.6.0

# 批次 Monkey 报告导出 PDF
weasyprint>=60.0

# 运行时模型 (shared.core.trom)
pydantic>=2.0.0

# XML解析 (UI Automation Relative XPath)
lxml>=4.9.0

# 测试用例 XMind 互转
xmindparser>=1.0.9
XMind>=1.2.0

# PRD 评审：Word 解析与报告导出
python-docx>=1.0.0

# 可选：Streamlit 播放器压测仪表盘
# streamlit>=1.28.0
# pandas>=1.5.0
