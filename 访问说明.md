# 用例管理系统访问说明

## ✅ 正确的访问方式

### 方式1：通过 trae_platform 统一平台（推荐）

1. **访问首页**：http://localhost:5000/
2. **点击左侧侧边栏的"用例管理"**（在"自动化测试"分组下）
3. 或直接访问：http://localhost:5000/test_case/

### 方式2：直接访问URL

**重要：URL末尾必须有斜杠！**

✅ **正确**：http://localhost:5000/test_case/
✅ **正确**：http://127.0.0.1:5000/test_case/

❌ **错误**：http://localhost:5000/test_case（缺少尾部斜杠，会404）

## 🔍 如何确认服务器正在运行

### trae_platform（端口5000）
- 访问：http://localhost:5000/
- 如果能看到首页，说明服务器在运行

### monkey独立应用（端口8083）
- 访问：http://localhost:8083/
- 这是另一个独立的服务器

## ⚠️ 常见问题

### Q1: 访问 /test_case/ 还是404
**解决方案：**
1. 确认URL末尾有斜杠：`/test_case/`（不是 `/test_case`）
2. 确认访问的是端口5000（不是8083）
3. 清除浏览器缓存，强制刷新（Ctrl+F5）

### Q2: 侧边栏没有"用例管理"
**解决方案：**
1. 确认访问的是 http://localhost:5000/（trae_platform）
2. 不是 http://localhost:8083/（monkey独立应用）
3. 如果确实没有，检查浏览器控制台是否有错误

### Q3: 服务器没有启动
**解决方案：**
```bash
cd d:\trae-code\trae_platform
python app.py
```

看到以下信息说明启动成功：
```
============================================================
Trae Platform 正在启动...
============================================================
访问地址: http://127.0.0.1:5000
访问地址: http://localhost:5000
============================================================
```

## 📝 验证步骤

1. ✅ 访问 http://localhost:5000/ 能看到首页
2. ✅ 左侧侧边栏有"用例管理"链接
3. ✅ 点击"用例管理"能正常跳转
4. ✅ 或直接访问 http://localhost:5000/test_case/ 能看到页面

## 🎯 快速测试

在浏览器中依次访问：
1. http://localhost:5000/ （应该看到首页）
2. http://localhost:5000/test_case/ （应该看到用例管理页面）

如果第1步成功但第2步404，请告诉我具体的错误信息。
